# Python 3.11+ required. Tested on 3.13.
#
# Lower bounds only: the analysis does not depend on behaviour that changed in
# any recent release, and pinning exactly would make the repo harder, not
# easier, for a reviewer to run. `requirements.lock.txt` carries the exact
# versions the Rotterdam baseline was produced with (pip freeze, Python 3.13).

# --- core geospatial -------------------------------------------------------
geopandas>=1.0
shapely>=2.0
pyogrio>=0.9          # vectorised GPKG/Parquet IO; much faster than fiona
pyproj>=3.6
rasterio>=1.3

# --- networks and morphometrics -------------------------------------------
osmnx>=2.0            # NOTE: v2 renamed several functions; v1.x will not work
networkx>=3.2
momepy>=0.7

# --- spatial statistics ----------------------------------------------------
libpysal>=4.9
esda>=2.5             # Moran's I and LISA for the clustering diagnostics
scipy>=1.11

# --- visualisation ---------------------------------------------------------
folium>=0.16
branca>=0.7           # colour ramps for the folium map (folium dep, pinned explicitly)
mapclassify>=2.6
matplotlib>=3.8
contextily>=1.5

# --- plumbing --------------------------------------------------------------
pandas>=2.2
numpy>=1.26
pyyaml>=6.0
tqdm>=4.66
requests>=2.31
